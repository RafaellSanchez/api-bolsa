name: Execute Python Script
on:
  workflow_dispatch:  # Acionamento manual

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Run Python script
        run: python teste_scheduler.py
      
